export const Names = Object.freeze({
  AUTH: Object.freeze({
    INDEX: "auth:index",
    LOGIN: "auth:login",
  }),
  HOME: Object.freeze({
    INDEX: "home:index",
  }),
  VACANCY: Object.freeze({
    INDEX: "vacancy:index",
  }),
});
